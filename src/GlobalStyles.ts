import styled from "styled-components";

export const Main = styled.main`
    display: grid;
    grid-template-columns: 1fr;
    max-width: 800px;
    margin: auto;

    padding: 20px;

    @media screen {
        
    }

`

